import {CART} from './cart';
import {CLIENT} from './client';
import {ACCESS_TOKEN, SHOP_NAME, DOMAIN} from './constants';

const Mocks = {
  CART,
  CLIENT,
  ACCESS_TOKEN,
  SHOP_NAME,
  DOMAIN,
};

export {Mocks};
